<h1>Cats</h1>
<%#= render 'cat', cat: cat %>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <%= cat.name %>
      <br>
      <a href="<%= cat_url(cat) %>">Find more about <%= cat.name %></a>
    </li>
    <br>
  <% end %>
</ul>